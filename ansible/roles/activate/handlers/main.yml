# {{ ansible_managed }}
- name: restart the idle timer
  ansible.builtin.systemd_service:
    name: vllm-idle.timer
    state: restarted
    daemon_reload: true
  when: inventory_hostname in groups['head']
