# Defect register

**Living index — not a decision record.** One row per **open** defect the cluster is
carrying, each with a **clears-when** condition so an update knows what to re-test.
This is the roll-up: the detailed analysis lives in the linked home (an upgrade
tracker's WAR register, an ADR, a model fact sheet). Rows **link, they don't
duplicate** — edit the home for detail, this table for status.

**Scope.** Defects whose follow-up is tied to an **update** — an upstream fix landing,
or a container/model/vLLM version bump we can re-test against. Operational gotchas with
a *permanent* fix (missing `--cgroupns=host`, `VLLM_HOST_IP` per node) are not defects
in this sense — they live in the README "Troubleshooting" section. A **model we have
rejected outright** is not a defect either: nothing here works around it, so its verdict
and revisit condition live in [`models/tombstones.md`](models/tombstones.md) — that file
owns them. (DEF-0008, `Qwen3.5-122B-A10B-FP8`, moved there on 2026-08-08 for exactly
this reason.) Optimization-class re-tests (disabled perf knobs) are governed by [ADR-0014](adr/0014-optimization-register.md);
the two that are corruption-bugs are indexed here as well (DEF-0007).

**How to use it during an update.** [`updating.md`](updating.md) sends you here: filter
to the component you're bumping, and for every row whose *Clears when* the update
satisfies, **re-test one at a time** (pulling several WARs at once hides which was still
load-bearing). Update the row's status with the result; when a defect is truly gone,
delete the row (git history keeps it) and drop its WAR from the home.

**Status:** 🔴 open (no mitigation, blocking) · 🟡 WAR'd (worked around, watching
upstream) · 🔵 watch (re-test on the next relevant bump — may already be fixed).

| ID | Symptom | Affects | Upstream | Workaround in place | Clears when — **re-test** | Status | Detail |
|---|---|---|---|---|---|---|---|
| **DEF-0002** | TP=2 inference-time CUDA deadlock, 35–55 min into serving (2× Spark) | sustained TP=2 serving (`minimax-m2.7-nvfp4`, `step-3.5-flash-fp8`) | [vllm#41725](https://github.com/vllm-project/vllm/issues/41725) | none confirmed — PP=2 sidesteps (pipeline-bubble latency) | #41725 fixed/closed — **soak-test** TP=2 for hours *under concurrency* (duration alone is not the gate: a 90-min *light-load* soak passed 2026-08-06 and proved nothing) | 🔵 watch — **not reproduced on 26.07.** 2026-08-08: 64 min, concurrency 8, **1312/1312 HTTP 200**, p50 21.8 s / max 23.4 s with no latency creep, straight through the 35–55 min window. Two clean soaks now (26.06 light, 26.07 concurrent); still filed because we have never seen the deadlock ourselves, so absence of reproduction is not a fix | [26.06 tracker](upgrades/container-nvidia-vllm-26.06-py3.md) · [26.07 tracker](upgrades/container-nvidia-vllm-26.07-py3.md) |
| **DEF-0003** | GB10 cudagraph inference hang (`FULL_AND_PIECEWISE`) | profiles once serving **that do not use speculative decoding** | [vllm#40969](https://github.com/vllm-project/vllm/issues/40969) | **none deliberate — masked by accident.** No profile sets `cudagraph_mode`/`--enforce-eager`; the MTP profile uses spec-decode, and vLLM auto-downgrades `FULL_AND_PIECEWISE`→`PIECEWISE` for spec-decode on FlashInfer. Remove spec-decode and the mask goes with it | #40969 fixed — re-test without spec-decode, then A/B throughput (ADR-0014). **First real exercise 2026-08-08 on 26.07: no hang** (see status) | 🔵 watch — **not reproduced on 26.07** | [26.06 tracker](upgrades/container-nvidia-vllm-26.06-py3.md) · [ADR-0014](adr/0014-optimization-register.md) |
| **DEF-0004** | compressed-tensors **WNA16 Marlin MoE weight-load hang** on sm_121 — clean 0.19→0.22.1 regression | any AWQ/Marlin MoE checkpoint on 26.06+ — no such profile remains | related: [vllm#40357](https://github.com/vllm-project/vllm/issues/40357) (closest), [#43906](https://github.com/vllm-project/vllm/issues/43906), [#35303](https://github.com/vllm-project/vllm/issues/35303), [#41511](https://github.com/vllm-project/vllm/issues/41511); exact load-hang **unfiled** | keep AWQ/Marlin models on **26.04** (NVFP4 uses `modelopt`, not Marlin — orthogonal) | the Marlin-MoE sm_121 load path is fixed in the vLLM build a container ships — **re-test** the model solo on the new 26.x | 🔴 **open — escalated 2026-08-08.** vLLM 0.24.0 (26.07) does **not** fix it, and the failure mode is now worse: MiniMax-M2.7-AWQ TP=2 at `gmu 0.75` exhausted host RAM at `Loading safetensors … 0/27` and **hard-froze sparky** (recovered only by power cycle; ADR-0009's boot gate then held the node empty and reachable — its first real firing). Not a headroom problem: `minimax-m2.7-nvfp4` loads a **larger** checkpoint (130.28 GiB) with **less** free RAM (42–45 GiB) and is fine. AWQ is pinned to 26.04 indefinitely; `minimax-m2.7-nvfp4` is now MiniMax's forward path. **Do not re-test AWQ on a new container without the fail-safe verified and a human present** — it is a node-killer, not a hang | [26.06 tracker appendix](upgrades/container-nvidia-vllm-26.06-py3.md) · [26.07 tracker](upgrades/container-nvidia-vllm-26.07-py3.md) |
| **DEF-0006** | Step-3.7 `Step3VLProcessor` crash-loops on startup — `AttributeError: … '_get_num_multimodal_tokens'` (VL-processor, orthogonal to NVFP4/container) | `step-3.7-flash-nvfp4` profile (`blocked: true` — weights kept, not activatable) | **unfiled** — vLLM `Step3VLProcessor` / `transformers/multimodal.py` | **unblock path found (2026-07 sweep):** StepFun ships a prebuilt image `vllm/vllm-openai:stepfun37` + an official vLLM Step-3.7 recipe — buildable/pullable via the `images` role (ADR-0013) | deploy on a container carrying the fixed processor (StepFun `stepfun37` image, or a stock vLLM once the method lands) — **re-test** `step-3.7-flash-nvfp4` | 🟡 WAR'd (container path exists) — **re-probed 2026-08-10 on 26.07: still missing.** `sparky probe attr vllm.model_executor.models.step3_vl Step3VLProcessor._get_num_multimodal_tokens` → `false`. The profile had been bumped to 26.07 while the register still carried a 26.06 verdict, so its status was stale evidence about a container it no longer runs. Re-taking this now costs ~20 s and no activation (ADR-0019), so it should be re-probed on **every** container bump rather than waiting for a sweep | [profile-step-3.7 tracker](upgrades/profile-step-3.7-flash.md) |
| **DEF-0007** | FP8 KV cache + prefix caching → multi-turn corruption (Nth-turn garbage / nonstop thinking) on vLLM 0.19 | **nothing — `step-3.5-flash-fp8` was retired 2026-08-10** and 26.04 left with it; the whole fleet is 0.24.0, where this did not reproduce | none filed — suspected FP8-KV × prefix-cache interaction, vLLM 0.19 | **none in place, and the row was wrong twice.** vLLM auto-enables FP8 KV from `kv_cache_quant_algo`, and V1 enables prefix caching BY DEFAULT — omitting `--enable-prefix-caching` does nothing. So `minimax-m2.7-nvfp4` and `qwen3-vl-235b-a22b-instruct-nvfp4` run **both halves live** | **TESTED 2026-08-09 — did not reproduce.** 30 turns of deliberately GROWING context against `qwen3-vl-235b-a22b-instruct-nvfp4` with `kv_cache_dtype=fp8_e4m3` and `enable_prefix_caching=True`: zero corruption flags, and recall stayed exact across the whole conversation (first arithmetic answer `68` recalled correctly at turn 21, ten turns later; first topic at turn 15; the language list consistent at turns 7 and 23). Short-turn tests cannot show this — prefix caching only reuses blocks once history is long enough to share them | 🟢 **CLOSED 2026-08-10 — unreproducible and now untestable.** The stated close condition was to re-run the 30-turn test on `step-3.5-flash-fp8` (26.04, vLLM 0.19), the only place the bug was ever observed. That profile was retired the same day and 26.04 left the fleet with it, so the condition can never be met. What we do know: the FP8-KV × prefix-cache pairing ran clean for 30 growing-context turns on 0.24.0, and every remaining profile is 0.24.0. Closing rather than watching forever — a defect no configuration can exhibit is register debt. If Step returns (`step-3.7-flash-nvfp4` is parked on DEF-0006), re-test then | [ADR-0014](adr/0014-optimization-register.md) |
| **DEF-0009** | `nvidia_gpu_exporter` crash-loops on GB10 — its `clocks_event_reasons_* "[us]"` fields become invalid Prometheus metric names | the GPU exporter on both nodes | **unfiled** — GB10-specific `nvidia-smi` output | pinned to **1.2.0** with an explicit safe query set (`nvidia_exporter_query_fields`, `group_vars/all.yml`) | a release handles the `[us]`-suffixed fields — **re-test** by bumping and dropping the query-field override. Note 1.13.1 is available but `--query-field-names` is absent from its README, so verify the flag still exists before bumping | 🟡 WAR'd | `group_vars/all.yml` · [version-discovery](../skills/version-discovery/SKILL.md) |
| **DEF-0010** | **All tool-calling 500s on 26.07** — `ImportError: cannot import name 'normalize_tool_choice' from 'xgrammar'` in `vllm/tool_parsers/structural_tag_registry.py`. `tool_choice: none` works; **`auto` and `required` both fail**, and Open WebUI sends `auto` on ordinary chats, so this breaks normal chat in the UI | every profile on `26.07-py3` with `--enable-auto-tool-choice` | **unfiled (NVIDIA packaging)** — the image ships **xgrammar 0.2.0** while vLLM 0.24.0 *requires* `xgrammar >= 0.2.1` (requirements/common.txt). Not a loose floor: NVIDIA shipped **below vLLM's own declared minimum**, which a `pip check` in the image would have caught | derived image **`dgx-spark/vllm:26.07-xgrammar-fix`** — 26.07 + `pip install --no-deps 'xgrammar>=0.2.1'` (→ 0.2.4), built on every node by the `images` role. **`--no-deps` is load-bearing**: without it pip re-resolves xgrammar's own `transformers` dep down to v4, which vLLM 0.24.0 removed support for, and every engine dies at import. Two build-time asserts now gate the image. It replaces the 26.06-fastapi-fix image whose defect (DEF-0005) 26.07 fixed | NVIDIA ships a 26.07+ image whose xgrammar has `normalize_tool_choice`, **or** vLLM tightens the floor — **re-test** with the smoke gate's tool-shape probe, which is what caught this | 🟡 WAR'd — **verified 2026-08-08**: `none`/`auto` 200, `auto` returns a real `tool_call`. (`required` still 500, but for an unrelated reason — see DEF-0011) | [26.07 tracker](upgrades/container-nvidia-vllm-26.07-py3.md) |

| **DEF-0011** | **Constrained tool choice 500s when MTP spec-decode is on.** `tool_choice: "required"` and named-function requests die mid-generation with `Failed to advance FSM … grammar rejected tokens [198, …]`; `none` and `auto` are fine, so ordinary Open WebUI chat is unaffected | 26.07 profiles combining a `--reasoning-parser` with `--speculative-config` — **no profile remains** — `qwen3.6-35b-a3b-nvfp4-mtp3-single` was retired 2026-08-10 (it measured slowest of the three qwen3.6 shapes; ADR-0014 errata), which removes the only profile that paired a reasoning parser with spec-decode | [vllm#34650](https://github.com/vllm-project/vllm/issues/34650) (MTP breaks `</think>` detection in structured output); same class as [#31954](https://github.com/vllm-project/vllm/issues/31954) | none applied. Callers needing a constrained shape can pass `chat_template_kwargs: {"enable_thinking": false}`, which makes all four shapes pass. Dropping MTP would also fix it, at 2.3× single-stream decode (ADR-0014) — not worth it for a shape Open WebUI never sends | #34650 fixed — **re-test** all four tool-choice shapes on the MTP profile | 🔵 watch — **CONFIRMED by controlled experiment 2026-08-08.** `qwen3.6-35b-a3b-nvfp4-single` is the *same weights, same container, same parsers* as the MTP profile with spec-decode removed, and it passes **all four** shapes where the sibling 500s on two. MTP is the cause, not a correlate. (`minimax-m2.7-nvfp4` agreed across models earlier; this is the same-model version.) | [26.07 tracker](upgrades/container-nvidia-vllm-26.07-py3.md) · [ADR-0014](adr/0014-optimization-register.md) |

| **DEF-0012** | **Mistral-Medium-3.5 crash-loops on startup** — `ValueError: Mismatch in 'image' token count between text and input_ids. Got ids=[0] and text=[1]` from `transformers/models/pixtral/processing_pixtral.py`, during vLLM's multimodal profiling. The engine never serves, text included | `mistral-medium-3.5-128b-nvfp4` on 26.07 (vLLM 0.24.0) | **unfiled — and probably a checkpoint packaging problem, not a vLLM bug.** Root-caused 2026-08-08: the checkpoint ships BOTH HF and Mistral-native artifacts, and the two paths disagree. `--tokenizer-mode mistral` uses tekken, which does not emit the `[IMG]` token id that HF's `PixtralProcessor` expects from the prompt text — so the processor sees one image in the text and zero in the ids. Dropping to the HF tokenizer is not an escape: vLLM then refuses the model outright (`must be an instance of MistralTokenizer`) | ❌ `--limit-mm-per-prompt {"image":0}` **does NOT work** — vLLM runs multimodal profiling regardless of the limit. **Next candidate:** add `--config-format mistral` so BOTH halves use the native path, as vLLM's own Mistral recipe prescribes them together; the hybrid state is ours, not upstream's. Risk: config-format mistral reads `params.json` and may lose the modelopt quant config | the native config path loads and serves — **or** stage a coherently-packaged build (official Mistral-native FP8, or a pure-HF NVFP4) instead. This checkpoint may simply be mis-assembled | 🔴 open | [bring-up failures](bring-up-failures.md) · [mistral fact sheet](models/mistral-medium-3.5.md) |
| **DEF-0013** | `Qwen3-VL-32B-Instruct-NVFP4` refused at config validation — its `quant_algo` is **`NVFP4_AWQ`**, and ModelOpt in this build accepts only `FP8 · FP8_PER_CHANNEL_PER_TOKEN · FP8_PB_WO · NVFP4 · W4A16_NVFP4 · MXFP8 · MIXED_PRECISION` | **no profile remains** — `qwen3-vl-32b-instruct-nvfp4-single` was retired 2026-08-10 (its niche closed, not its defect: re-probed the same day, still no `NVFP4_AWQ`). The 235B sibling is plain NVFP4 and unaffected | **unfiled** — vLLM's ModelOpt algo allowlist. Note `modelopt_fp4` *is* among vLLM's quantization **methods**; the algo list is a different, shorter one, and confusing the two is what made this look supported | profile **retired** to `ansible/profiles/retired/`, weights evicted. The config is kept so a revival costs no re-derivation; see [tombstones](models/tombstones.md) | `./sparky.sh probe quant` lists `NVFP4_AWQ` under `modelopt_algos` — **or** stage a plain-NVFP4/FP8 build of the same model instead | 🔵 watch | `ansible/profiles/qwen3-vl-32b-instruct-nvfp4-single.yml` |

| **DEF-0014** | **DeepSeek-V4-Flash does not start on this container** — `Unknown SF transformation` (`layout.hpp`) at weight load. The FP4 **expert-scale** path has no sm_121 kernels; FP8 works on SM 12.x, so the name NVFP4 misleads — the weights are FP8 dense + mxfp4 MoE | `deepseek-v4-flash-0731` — **parked** (`blocked: true`). The official 0731 release supersedes the DSpark preview and is a drop-in (identical structure and config); **166.9 GB staged and mirrored to both nodes 2026-08-11**, each verified file-by-file against the Hub, so unblocking is a flag change and an activation, not a download | **FILED UPSTREAM, and we had the cause backwards.** [vllm#41063](https://github.com/vllm-project/vllm/issues/41063) tracks SM 12.x coverage and states plainly that **FP8 operations work** — the gaps are FP4: `FP4 GEMM dispatch only knows sm90_fp8_gemm_* and sm100_fp8_fp4_gemm_1d1d` (routing to SM100 emits `tcgen05` ISA that sm_121 cannot execute), FP4 attention kernels absent, FP4 einsum missing. [DeepGEMM#372](https://github.com/deepseek-ai/DeepGEMM/issues/372) is our exact error: no SM120/121 branch for `(gran_mn=1, gran_k=32)` **NVFP4 expert scales**, which blocks weight loading — matching `Unknown SF transformation` at `layout.hpp`. DeepSeek-V4-Flash is FP8 dense + **mxfp4 MoE**, so the FP4 experts are what we hit. Kernel coverage, not configuration — but of the FP4 path | none possible on NGC. Every working GB10 configuration replaces the engine with the `jasl/vllm` fork; `VLLM_USE_DEEP_GEMM=0` only exposed the second missing path | a container carries the **sm_12x DeepSeek-V4 model path** — then drop `blocked:` and activate. **Watch [vllm#41834](https://github.com/vllm-project/vllm/pull/41834)** (open, active), NOT #41063 (quiet, 4 comments). The kernels themselves already landed in [DeepGEMM#324](https://github.com/deepseek-ai/DeepGEMM/pull/324) 2026-06-24 | 🔵 watch — **deferred 2026-08-11, deliberately.** Building the fork needs ~107 GB free RAM off a serving node, the flags need `deepseek_v4` parsers 0.24.0 lacks, and the payoff is ~40 t/s decode against the incumbent's 100.2 | [bring-up failures](bring-up-failures.md) |

## Filing a new defect

When a deploy/bench/smoke turns up a defect worth carrying (an upstream bug we work
around, a model/version that breaks, a corruption class): add a row here with the next
`DEF-NNNN`, and put the detail in its proper home ([documentation skill](../skills/documentation/SKILL.md):
a WAR row in the relevant upgrade tracker, an ADR-0014 register row, or a model fact
sheet). Back-reference the ID from the code comment or profile that carries the WAR
(`# WAR for DEF-0005 …`) so the mitigation points at its tracking row. Keep the
*Clears when* concrete — that condition is what makes follow-up efficient.
