# Defect register

**Living index — not a decision record.** One row per **open** defect the cluster is
carrying, each with a **clears-when** condition so an update knows what to re-test.
This is the roll-up: the detailed analysis lives in the linked home (an upgrade
tracker's WAR register, an ADR, a model fact sheet). Rows **link, they don't
duplicate** — edit the home for detail, this table for status.

**Scope.** Defects whose follow-up is tied to an **update** — an upstream fix landing,
or a container/model/vLLM version bump we can re-test against. Operational gotchas with
a *permanent* fix (missing `--cgroupns=host`, `VLLM_HOST_IP` per node) are not defects
in this sense — they live in the README "Troubleshooting" section. A **model we have
rejected outright** is not a defect either: nothing here works around it, so its verdict
and revisit condition live in [`models/tombstones.md`](models/tombstones.md) — that file
owns them. (DEF-0008, `Qwen3.5-122B-A10B-FP8`, moved there on 2026-08-08 for exactly
this reason.) Optimization-class re-tests (disabled perf knobs) are governed by [ADR-0014](adr/0014-optimization-register.md);
the two that are corruption-bugs are indexed here as well (DEF-0007).

**How to use it during an update.** [`updating.md`](updating.md) sends you here: filter
to the component you're bumping, and for every row whose *Clears when* the update
satisfies, **re-test one at a time** (pulling several WARs at once hides which was still
load-bearing). Update the row's status with the result; when a defect is truly gone,
delete the row (git history keeps it) and drop its WAR from the home.

**Status:** 🔴 open (no mitigation, blocking) · 🟡 WAR'd (worked around, watching
upstream) · 🔵 watch (re-test on the next relevant bump — may already be fixed).

| ID | Symptom | Affects | Upstream | Workaround in place | Clears when — **re-test** | Status | Detail |
|---|---|---|---|---|---|---|---|
| **DEF-0002** | TP=2 inference-time CUDA deadlock, 35–55 min into serving (2× Spark) | sustained TP=2 serving (`minimax-m2.7-nvfp4`, `step-3.5-fp8`) | [vllm#41725](https://github.com/vllm-project/vllm/issues/41725) | none confirmed — PP=2 sidesteps (pipeline-bubble latency) | #41725 fixed/closed — **soak-test** TP=2 for hours *under concurrency* (duration alone is not the gate: a 90-min *light-load* soak passed 2026-08-06 and proved nothing) | 🔵 watch — **not reproduced on 26.07.** 2026-08-08: 64 min, concurrency 8, **1312/1312 HTTP 200**, p50 21.8 s / max 23.4 s with no latency creep, straight through the 35–55 min window. Two clean soaks now (26.06 light, 26.07 concurrent); still filed because we have never seen the deadlock ourselves, so absence of reproduction is not a fix | [26.06 tracker](upgrades/container-nvidia-vllm-26.06-py3.md) · [26.07 tracker](upgrades/container-nvidia-vllm-26.07-py3.md) |
| **DEF-0003** | GB10 cudagraph inference hang (`FULL_AND_PIECEWISE`) | profiles once serving **that do not use speculative decoding** | [vllm#40969](https://github.com/vllm-project/vllm/issues/40969) | **none deliberate — masked by accident.** No profile sets `cudagraph_mode`/`--enforce-eager`; the MTP profile uses spec-decode, and vLLM auto-downgrades `FULL_AND_PIECEWISE`→`PIECEWISE` for spec-decode on FlashInfer. Remove spec-decode and the mask goes with it | #40969 fixed — re-test without spec-decode, then A/B throughput (ADR-0014). **First real exercise 2026-08-08 on 26.07: no hang** (see status) | 🔵 watch — **not reproduced on 26.07** | [26.06 tracker](upgrades/container-nvidia-vllm-26.06-py3.md) · [ADR-0014](adr/0014-optimization-register.md) |
| **DEF-0004** | compressed-tensors **WNA16 Marlin MoE weight-load hang** on sm_121 — clean 0.19→0.22.1 regression | any AWQ/Marlin MoE checkpoint on 26.06+ — no such profile remains | related: [vllm#40357](https://github.com/vllm-project/vllm/issues/40357) (closest), [#43906](https://github.com/vllm-project/vllm/issues/43906), [#35303](https://github.com/vllm-project/vllm/issues/35303), [#41511](https://github.com/vllm-project/vllm/issues/41511); exact load-hang **unfiled** | keep AWQ/Marlin models on **26.04** (NVFP4 uses `modelopt`, not Marlin — orthogonal) | the Marlin-MoE sm_121 load path is fixed in the vLLM build a container ships — **re-test** the model solo on the new 26.x | 🔴 **open — escalated 2026-08-08.** vLLM 0.24.0 (26.07) does **not** fix it, and the failure mode is now worse: MiniMax-M2.7-AWQ TP=2 at `gmu 0.75` exhausted host RAM at `Loading safetensors … 0/27` and **hard-froze sparky** (recovered only by power cycle; ADR-0009's boot gate then held the node empty and reachable — its first real firing). Not a headroom problem: `minimax-m2.7-nvfp4` loads a **larger** checkpoint (130.28 GiB) with **less** free RAM (42–45 GiB) and is fine. AWQ is pinned to 26.04 indefinitely; `minimax-m2.7-nvfp4` is now MiniMax's forward path. **Do not re-test AWQ on a new container without the fail-safe verified and a human present** — it is a node-killer, not a hang | [26.06 tracker appendix](upgrades/container-nvidia-vllm-26.06-py3.md) · [26.07 tracker](upgrades/container-nvidia-vllm-26.07-py3.md) |
| **DEF-0006** | Step-3.7 `Step3VLProcessor` crash-loops on startup — `AttributeError: … '_get_num_multimodal_tokens'` (VL-processor, orthogonal to NVFP4/container) | `step-3.7-nvfp4` profile (`blocked: true` — weights kept, not activatable) | **unfiled** — vLLM `Step3VLProcessor` / `transformers/multimodal.py` | **unblock path found (2026-07 sweep):** StepFun ships a prebuilt image `vllm/vllm-openai:stepfun37` + an official vLLM Step-3.7 recipe — buildable/pullable via the `images` role (ADR-0013) | deploy on a container carrying the fixed processor (StepFun `stepfun37` image, or a stock vLLM once the method lands) — **re-test** `step-3.7-nvfp4` | 🟡 WAR'd (container path exists) — **re-verified 2026-08-08: 26.06's `Step3VLProcessor` still lacks the method** | [profile-step-3.7 tracker](upgrades/profile-step-3.7-flash.md) |
| **DEF-0007** | FP8 KV cache + prefix caching → multi-turn corruption (Nth-turn garbage / nonstop thinking) on vLLM 0.19 | `step-3.5-fp8` (and FP8/AWQ on 26.04) | none filed — suspected FP8-KV × prefix-cache interaction, vLLM 0.19 | both disabled on `step-3.5-fp8` | **re-test on 26.07 (vLLM 0.24.0)**, which means moving `step-3.5-fp8` off 26.04 first — the bug's container is behind us, may already be fixed; enable one at a time, multi-turn test (ADR-0014 method) | 🔵 watch | [ADR-0014](adr/0014-optimization-register.md) · README "Known Shortcomings" |
| **DEF-0009** | `nvidia_gpu_exporter` crash-loops on GB10 — its `clocks_event_reasons_* "[us]"` fields become invalid Prometheus metric names | the GPU exporter on both nodes | **unfiled** — GB10-specific `nvidia-smi` output | pinned to **1.2.0** with an explicit safe query set (`nvidia_exporter_query_fields`, `group_vars/all.yml`) | a release handles the `[us]`-suffixed fields — **re-test** by bumping and dropping the query-field override. Note 1.13.1 is available but `--query-field-names` is absent from its README, so verify the flag still exists before bumping | 🟡 WAR'd | `group_vars/all.yml` · [version-discovery](../skills/version-discovery/SKILL.md) |
| **DEF-0010** | **All tool-calling 500s on 26.07** — `ImportError: cannot import name 'normalize_tool_choice' from 'xgrammar'` in `vllm/tool_parsers/structural_tag_registry.py`. `tool_choice: none` works; **`auto` and `required` both fail**, and Open WebUI sends `auto` on ordinary chats, so this breaks normal chat in the UI | every profile on `26.07-py3` with `--enable-auto-tool-choice` | **unfiled (NVIDIA packaging)** — the image ships **xgrammar 0.2.0** while vLLM 0.24.0 *requires* `xgrammar >= 0.2.1` (requirements/common.txt). Not a loose floor: NVIDIA shipped **below vLLM's own declared minimum**, which a `pip check` in the image would have caught | derived image **`dgx-spark/vllm:26.07-xgrammar-fix`** — 26.07 + `pip install --no-deps 'xgrammar>=0.2.1'` (→ 0.2.4), built on every node by the `images` role. **`--no-deps` is load-bearing**: without it pip re-resolves xgrammar's own `transformers` dep down to v4, which vLLM 0.24.0 removed support for, and every engine dies at import. Two build-time asserts now gate the image. It replaces the 26.06-fastapi-fix image whose defect (DEF-0005) 26.07 fixed | NVIDIA ships a 26.07+ image whose xgrammar has `normalize_tool_choice`, **or** vLLM tightens the floor — **re-test** with the smoke gate's tool-shape probe, which is what caught this | 🟡 WAR'd — **verified 2026-08-08**: `none`/`auto` 200, `auto` returns a real `tool_call`. (`required` still 500, but for an unrelated reason — see DEF-0011) | [26.07 tracker](upgrades/container-nvidia-vllm-26.07-py3.md) |

| **DEF-0011** | **Constrained tool choice 500s when MTP spec-decode is on.** `tool_choice: "required"` and named-function requests die mid-generation with `Failed to advance FSM … grammar rejected tokens [198, …]`; `none` and `auto` are fine, so ordinary Open WebUI chat is unaffected | 26.07 profiles combining a `--reasoning-parser` with `--speculative-config` — today **only** `qwen3.6-35b-nvfp4-mtp3-single` | [vllm#34650](https://github.com/vllm-project/vllm/issues/34650) (MTP breaks `</think>` detection in structured output); same class as [#31954](https://github.com/vllm-project/vllm/issues/31954) | none applied. Callers needing a constrained shape can pass `chat_template_kwargs: {"enable_thinking": false}`, which makes all four shapes pass. Dropping MTP would also fix it, at 2.3× single-stream decode (ADR-0014) — not worth it for a shape Open WebUI never sends | #34650 fixed — **re-test** all four tool-choice shapes on the MTP profile | 🔵 watch — **scope confirmed by experiment 2026-08-08**: `minimax-m2.7-nvfp4` (same container, same reasoning-parser design, **no** spec-decode) passes all four shapes, so reasoning alone is not sufficient; MTP is the co-factor | [26.07 tracker](upgrades/container-nvidia-vllm-26.07-py3.md) · [ADR-0014](adr/0014-optimization-register.md) |

## Filing a new defect

When a deploy/bench/smoke turns up a defect worth carrying (an upstream bug we work
around, a model/version that breaks, a corruption class): add a row here with the next
`DEF-NNNN`, and put the detail in its proper home ([documentation skill](../skills/documentation/SKILL.md):
a WAR row in the relevant upgrade tracker, an ADR-0014 register row, or a model fact
sheet). Back-reference the ID from the code comment or profile that carries the WAR
(`# WAR for DEF-0005 …`) so the mitigation points at its tracking row. Keep the
*Clears when* concrete — that condition is what makes follow-up efficient.
