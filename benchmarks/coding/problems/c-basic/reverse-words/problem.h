#ifndef PROBLEM_H
#define PROBLEM_H

typedef __SIZE_TYPE__ size_t;

/* Reverse the word order of a single-space-separated string, in place. Returns the word
   count. The string keeps its length and stays NUL-terminated. */
size_t reverse_words(char *s);

#endif
